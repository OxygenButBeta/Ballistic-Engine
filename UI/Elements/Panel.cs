namespace BallisticEngine.UI;

// A plain container element — the analogue of an HTML <div> / Unity's bare VisualElement. Carries
// no content of its own; it exists to group children and hold layout + background/border style.
// The UXML loader maps <Panel> (and bare <VisualElement>) to this.
public class Panel : VisualElement
{
}
