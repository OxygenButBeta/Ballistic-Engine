# Build output (any project, any depth)
[Bb]in/
[Oo]bj/

# Regenerable asset import cache
SampleProject/Library/

# Local Claude Code session settings
.claude/

# ImGui editor layout persistence (per-machine)
imgui.ini

# Bistro test content (1.6 GB) — kept out of plain git until the git-lfs decision.
# Remove this line and `git add` the folder once LFS (or plain tracking) is chosen.
SampleProject/Assets/Default/Bistro_v5_2/
